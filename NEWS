v1.2.0
-added a labels argument that can accept a per-point vector to manually
add labels e.g. via ggrepel::geom_text_repel()

v1.1.0
-ggMAplot code simplification, now everything in dplyr/pipe style

v1.0.1
-replaced dplyr dependency by magrittr as we only need the pipe function actually

v1.0.0
-removed the theme_set options from plot_profiles so can be fully set by the user globally

v0.9.9
- putting together the package. Once the main functions are uploaded will make a v1.0.0 release.
- added ggMAplot and plot_profiles functions 
- added extdata 
